const { setupStartSessionResultRoute } = require('./startSessionRoutes/resultRoute');
const { setupStartSessionRoute } = require('./startSessionRoutes/sessionRoute');

module.exports = {
  setupStartSessionResultRoute,
  setupStartSessionRoute
};
